# ===========================================
# Better Auth Configuration
# ===========================================
# Secret key for signing tokens (generate with: openssl rand -base64 32)
BETTER_AUTH_SECRET=your-generated-secret-here

# Database URL for Better Auth (PostgreSQL)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# ===========================================
# Application URLs - CRITICAL: Set these correctly!
# ===========================================

# NEXT_PUBLIC_APP_URL: Where YOUR NEXT.JS APP runs (Vercel URL)
# - Auth routes (/api/auth/*) are served from here
# - Local: http://localhost:3000
# - Production: https://your-app.vercel.app
NEXT_PUBLIC_APP_URL=http://localhost:3000

# NEXT_PUBLIC_API_URL: Where YOUR BACKEND API runs (Hugging Face URL)
# - Task routes (/api/tasks/*) are served from here
# - Local: http://localhost:8000
# - Production: https://your-space.hf.space
NEXT_PUBLIC_API_URL=http://localhost:8000
